# External server port that will be published.
# Note: 6000 is blocked by Chrome/Edge as ERR_UNSAFE_PORT.
PUBLIC_PORT=3030

# Internal ports for containers (not published).
GATEWAY_PORT=6000
WEBAPP_PORT=8080
