# Docker Compose читает этот файл из корня проекта (рядом с docker-compose.yml).
# Полный набор переменных: deploy/server.env
PUBLIC_PORT=3030
VITE_ENABLE_EXTERNAL_NETWORK=true
VITE_DATA_SOURCE=yandex
